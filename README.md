# Оптимизатор текстур 41 — by issmaker

Рабочая версия 41: C++20, React/WebGL, Qt 6 и libdeflate. При запуске доступны два
сценария на базе AGR RGB24: фиксированная НПМ-оптимизация до 3 MB и пакетная
автоматическая оптимизация 2K/4K без ручного лимита размера.

В версии 36 C++ оставлен быстрым вычислительным движком, а интерфейс перенесён
в локальный Chromium/WebGL. Three.js строит объёмную форму геометрией в реальном
времени; bloom, частицы, свет и камера рассчитываются на GPU. React отвечает за
рабочие экраны, GSAP — за кинематографические переходы, а Qt WebChannel безопасно
связывает локальный web-интерфейс с C++ без внешнего сервера.

## Сборка Windows

### Автоматически — готовый portable EXE

Проект содержит workflow `.github/workflows/windows-portable.yml`. После
загрузки проекта в GitHub откройте **Actions → Build Windows Portable EXE → Run
workflow**. В результате появится artifact
`AdaptiveTextureOptimizer-41-Windows-Portable`. Его нужно скачать, распаковать
и запустить `AdaptiveTextureOptimizer.exe`. Python, Qt и Visual Studio на
компьютере пользователя не нужны: необходимые Qt DLL и MSVC runtime уже лежат
рядом с EXE.

### Локально

Установите Visual Studio 2022 (Desktop development with C++), CMake, Git и Qt
6.6+ с комплектом MSVC. Затем откройте `x64 Native Tools Command Prompt`:

```bat
cmake -S . -B build -G Ninja -DCMAKE_PREFIX_PATH=C:/Qt/6.8.3/msvc2022_64 -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release
C:/Qt/6.8.3/msvc2022_64/bin/windeployqt --qmldir qml build/AdaptiveTextureOptimizer.exe
```

Путь и версию Qt замените на установленные у вас. CMake самостоятельно загрузит
закреплённую версию libdeflate. Готовый EXE принимает PNG перетаскиванием на окно
или на сам файл программы.

## Один режим AGR Adaptive RGB24

Старая лаборатория из десяти режимов больше не используется. НПМ-сценарий запускает
один предсказуемый конвейер: сначала пробует lossless RGB24, затем адаптивно
уменьшает число цветовых представителей и уточняет результат бинарным поиском.
Для сложных изображений предусмотрены уровни вплоть до одного представителя,
поэтому допустимый предел размера не подменяется фиксированными 3 MB.

## Два рабочих сценария

**Оптимизация для НПМ-текстур** принимает один PNG, при необходимости готовит
рабочий 2K-эталон и всегда использует фиксированный предел 3 MB. Пользователь не
может случайно изменить обязательный для этого сценария лимит.

**Оптимизация текстур** принимает сразу несколько PNG 2K/4K и сохраняет их
разрешение. Здесь нет цели в мегабайтах: для каждого файла перебираются RGB24-
кандидаты, а самым лёгким считается только вариант, прошедший защитный порог
PSNR, средней и максимальной ошибки каналов. Ошибка одного файла отмечается в
списке и не останавливает остальные. Готовые файлы сохраняются в `compressed`,
после чего для каждого доступен отдельный сравнительный анализ.

## Изменения версии 41

- Сравнение переведено на viewport-canvas: при зуме и перетаскивании рисуется
  только видимая область, без гигантских GPU-слоёв и чёрного срыва интерфейса.
- Все реакции фона отделены от React-дерева рабочих экранов и ограничены одним
  кадром, поэтому drag больше не перерисовывает оболочку приложения.
- Убраны накопительное свечение, temporal distortion и скачки bloom; яркость
  сцены теперь детерминирована и не растёт во время простоя.
- Центральная тороидальная фигура заменена сигнальной решёткой, а крайние формы
  — спокойными кинетическими рамами.
- Переходы собраны заново: короткий портал, согласованный exit/enter и без
  конфликтующих keyframe-анимаций.
- Стартовая панель выбора увеличена, подпись `by issmaker` перенесена вниз по
  центру, а удержание получило плавный вход и возврат без elastic-рывка.

## Изменения версии 40

- устранён конфликт `ref` между tooltip и viewport, из-за которого не работал
  зум, drag выбрасывал JavaScript-ошибку, а подсказки появлялись слева сверху;
- подсказки переведены на единый фиксированный cursor-layer с ограничением по
  краям экрана;
- чёрная подложка сравнительных окон удалена: свободные поля вокруг текстуры
  показывают общий фон;
- с масштабируемых 2K-превью удалены дорогие filter/drop-shadow и принудительная
  GPU-растеризация, особенно опасные при масштабе 600%;
- drag ограничен по частоте, корректно завершается при потере pointer capture и
  временно приостанавливает фоновые циклы;
- WebGL работает только на главном экране; НПМ, batch и сравнение используют
  постоянную SVG-скульптуру, освобождая видеопамять для текстур;
- добавлена новая центральная vector-flow фигура и две морфирующие боковые формы;
- переходы сопровождаются втягиванием центральной геометрии, линий и боковых
  объектов в одну точку;
- яркость 3D-света и bloom ограничена, а рабочие заголовки получили постоянный
  тёмный contrast-layer — текст читается во всех пресетах;
- с главной карточки удалены заголовок «Два конвейера» и описательный абзац;
- Windows interaction smoke-test теперь программно проверяет wheel-zoom,
  координаты tooltip, смену маршрута и открытие настроек.

## Изменения версии 39

- WebGL изолирован React error boundary: ошибка GPU-сцены больше не может убрать
  рабочий интерфейс, а DOM/SVG-фон остаётся видимым всегда;
- 3D-композиция стала постоянной и не пересоздаётся при переходе между НПМ,
  списком и сравнением;
- добавлены Motion 13 для переходов-погружений и Floating UI для подсказок,
  которые выводятся portal-слоем и не обрезаются карточками;
- декодирование убрано из кнопок и навигации: размеры клика, текст и фон больше
  не дёргаются под курсором;
- верхняя шапка переработана, навигация вынесена в фигурный выдвижной dock
  справа;
- окна сравнения получили прозрачную композицию, тонкие светящиеся рамки,
  сглаженные края текстур и новый zoom-orbit с ромбовидным ползунком;
- зум и pan создают импульсы фона, света и линий, но не позволяют потерять
  изображение за границами;
- добавлены курсорный световой шлейф, hold-ритуал в точке нажатия, переходы
  между экранами с погружением и секретная сцена по нажатию `by issmaker`;
- добавлена панель настроек с четырьмя цветовыми пресетами, тремя уровнями
  эффектов и сохранением выбора между запусками;
- кнопка полноэкранного режима переключает приложение между настоящим fullscreen
  и развёрнутым оконным режимом;
- метрики прогресса стали круговыми, а линейные заполнения получили движущийся
  поток и световые полосы;
- startup smoke-test теперь автоматически открывает пакетный экран и настройки,
  проверяя переходы React внутри готового portable EXE;
- выделение и копирование декоративного текста интерфейса отключено.

## Изменения версии 38

- добавлена независимая от WebGL реактивная линейная подложка: фон остаётся
  видимым даже при восстановлении GPU-контекста во время сравнения больших PNG;
- batch-превью ограничены безопасным 2K-размером, поэтому две сравнительные
  текстуры не переполняют видеопамять Chromium;
- зум стал плавным и выполняется относительно курсора; масштаб не опускается
  ниже режима «Вписать», а панорамирование ограничено реальными краями картинки;
- зум и перемещение передают импульс фоновой геометрии, свету, камере и
  параллаксу;
- центральная незамкнутая тороидальная форма удалена и заменена гладкой
  многослойной torus-knot скульптурой без обрезанных концов;
- графики получили сетку, градиентную площадь, текущие значения, светящиеся
  точки и анимацию построения;
- RGB24, 8K READY и BATCH переделаны в полноценные информационные модули;
- добавлены контекстные подсказки, переработаны кнопки, навигация и иконка;
- подпись `by issmaker` отображается один раз — в правом нижнем углу.

## Изменения версии 37

- текстуры отдаются web-интерфейсу через локальный протокол `texture://`, поэтому
  Chromium корректно показывает исходник, 8K-preview и результат;
- светлые рабочие сцены удалены: НПМ, batch и сравнение получили разные тёмные
  WebGL-композиции с cyan, violet и magenta-освещением;
- рабочие поверхности стали полупрозрачными и оставляют 3D-дизайн видимым сверху,
  по краям и сквозь глубокое стекло;
- добавлены процедурные glass-morphism объекты с transmission, refraction,
  chromatic aberration и distortion;
- основное название сокращено до «Оптимизатор текстур»;
- в навигацию и hero-композицию добавлена светящаяся подпись `by issmaker`;
- главный экран получил дополнительные искры, стеклянные формы и технические
  RGB24/2K/4K/8K-индикаторы.

## Изменения версии 36

- QML-интерфейс заменён полноценным React-интерфейсом внутри Qt WebEngine;
- фон больше не является картинкой или Canvas-имитацией: форма состоит из
  настоящей Three.js-геометрии с physical materials, освещением и камерой;
- добавлены GPU bloom, noise, vignette, прозрачная сфера, искры и процедурная
  instanced-сетка из 126 частиц;
- hover текста запускает посимвольное декодирование и светящийся пиксельный след;
- удержание работает в любой свободной области только главного экрана;
- во время кинематографического пролёта текст и навигация полностью исчезают;
- C++ RGB24-движок доступен из JavaScript через локальный Qt WebChannel;
- устранена причина чёрного текста: цветовые токены теперь принадлежат каждой
  сцене и меняются вместе с её подложками.

## Изменения версии 35

- добавлена интерактивная сцена «нажмите и удерживайте» с четырьмя фазами,
  движением камеры, масштабом, поворотом, светом и смысловой типографикой;
- отпускание элемента плавно проигрывает сцену назад до исходной hero-композиции;
- режимы теперь переключают глобальную светлую или тёмную палитру, поверхности,
  навигацию и контраст, а не только декоративный фон;
- переходы между разделами закрываются полноэкранной цветовой шторкой;
- центральная форма меняет роль: hero-ленты, крупный тоннель и объект внутри
  светящейся сферы на экране сравнения;
- сохранены два рабочих конвейера, исправленный 8K Fit, графики, остановка,
  подсказки и мини-игра.

## Исправления версии 34

- устранён конфликт имени `Theme` со встроенной темой Qt Quick Controls;
- все цвета переведены в безопасный singleton `AppTheme`, поэтому текст больше
  не становится чёрным, а метрики — белыми из-за неразрешённых свойств;
- smoke-test трижды проверяет загрузку нового `AppTheme` внутри portable EXE;
- на рабочих экранах яркость объёмных лент уменьшена до 13%, а атмосферы — до
  42%; сильный hero-фон остался только на стартовом экране;
- окна сравнения получили плотную тёмную подложку и снова читаются поверх фона;
- исправлен контраст верхней навигации, подписей, карточек и выключенных состояний.

## Изменения версии 33

- прежняя бирюзовая спираль и декоративные волны полностью удалены;
- создан новый масштабируемый QML-фон с зерном, пурпурным светом и четырьмя
  объёмными лентами с отдельными тенями, телом, кромкой и бликом;
- левая панель заменена верхней web-навигацией в тёмной капсуле;
- стартовый экран перестроен как большой hero: крупная типографика слева и
  два ясных production-сценария справа;
- весь интерфейс переведён на единую чёрно-розово-пурпурную палитру;
- параллакс и медленное «дыхание» лент реагируют на движение указателя;
- обновлены знак приложения, Windows ICO, графики и мини-игра;
- функциональность RGB24, пакетная очередь, остановка и исправленный 8K Fit
  остались без изменений.

## Изменения версии 32

- полностью заменены самодельные Canvas-иконки на согласованный набор Lucide;
- узкий dock превращён в понятную подписанную боковую навигацию;
- добавлена единая система цветов, поверхностей, радиусов и типографики `Theme`;
- уменьшен визуальный шум: спокойнее границы, скругления, hover и стеклянные блики;
- стартовые карточки, кнопки, метрики, списки и индикаторы используют одну
  геометрию и одинаковую иерархию состояний;
- добавлены лицензионные уведомления и фиксация `lucide-static 1.30.0`;
- portable-пакет автоматически включает `THIRD_PARTY_NOTICES.md`.

## Изменения версии 31

- исправлен режим «Вписать» для 8K: окно сравнения использует лёгкое превью до
  3072 px, а полный исходник и полный результат остаются без изменения;
- пересчёт «Вписать» выполняется после готовности изображения и смены экрана;
- оформление заменено на Carbon/Teal Organic Flow по новым референсам;
- добавлен крупный многослойный спиральный элемент, усиленный параллакс и
  фиксированные бирюзовые, янтарные и коралловые акценты;
- карточки стали темнее, тоньше и ближе к графитовым dashboard-панелям;
- обновлена иконка приложения и название визуальной системы.

## Изменения версии 30

- приложение всегда открывается в настоящем полноэкранном режиме;
- auto-quality использует два быстрых качественных прохода и libdeflate 7;
- НПМ и пакетную очередь можно безопасно остановить во время обработки;
- масштаб больше не отдаляется значительно дальше режима «Вписать»;
- скруглённая маска не позволяет изображению выходить углами за окно;
- тема космоса и Марс полностью удалены;
- приложение использует разные Liquid/Aurora-фоны для выбора, НПМ и batch;
- добавлены рисованные QML-иконки, не зависящие от шрифтов Windows;
- dock выполнен в стиле iOS с подсвеченным выбранным разделом;
- стекло получило движущиеся блики, дополнительные границы и свечение;
- параллакс фоновых волн и сфер усилен;
- после каждого импорта автоматически выполняется «Вписать»;
- добавлена мини-игра «Поймай каплю» со счётчиком и выключателем.

Палитра существует только внутри оптимизатора. Перед PNG-кодированием изображение
всегда разворачивается обратно в `RGB888`; финальная проверка требует PNG color
type 2 и bit depth 8, то есть настоящий RGB24. Алгоритм увеличивает число
представителей бинарным поиском, пока следующий вариант уже не помещается в
заданный размер. Это тратит доступный бюджет качественно, без возврата случайных
исходных пикселей и цветных выбросов.

## Текущий конвейер

1. Декодирование в RGB24; 8K уменьшается напрямую до 2K.
2. Lossless-поиск фильтров None/Sub/Up/Paeth/adaptive MinSum.
3. Упаковка libdeflate уровня 10.
4. Если выбранный предел не достигнут — AGR Adaptive RGB24 ищет максимально богатое
   представление, которое реально проходит лимит.
5. После каждого кандидата измеряются размер, PSNR, mean ΔRGB и max ΔRGB.
6. Финальный PNG декодируется и сверяется с выбранным RGB-массивом.

## Автоматическая проверка

GitHub Actions собирает portable-комплект, трижды проверяет запуск QML-интерфейса
и отдельно выполняет сквозной self-test оптимизации: создаёт тестовый PNG,
обрабатывает его, проверяет предел размера, RGB24 и точное декодирование результата.
